# Server configuration
# HTTP port for Express + WS (insecure)
PORT=8787

# Enable HTTPS listener (1 to enable)
NDN_HTTPS=0
# HTTPS port (when enabled)
HTTPS_PORT=8788
# Paths to TLS key/cert if using custom locations
NDN_TLS_KEY=./certs/server.key
NDN_TLS_CERT=./certs/server.crt

# Logging level: trace|debug|info|warn|error|fatal|silent
LOG_LEVEL=info

# JWT Secret for token signing (REQUIRED)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# --- Stripe (optional if using Payment Link) ---
# Secret API key (do NOT set in Netlify). Set in Render only.
STRIPE_SECRET_KEY=
# Webhook secret if you verify events server-side (recommended)
STRIPE_WEBHOOK_SECRET=
# £2 username change price ID (provided by user)
STRIPE_PRICE_ID_USERNAME_CHANGE=price_1SC1iwRAvdcgMbFdqDMr39gu

# --- Email Provider (choose ONE) ---

# OPTION 1: Resend (RECOMMENDED for cloud hosts like Render)
# Sign up free at https://resend.com — 100 emails/day free tier
# 1. Create an API key at https://resend.com/api-keys
# 2. Set RESEND_API_KEY below
# 3. For custom "from" address, verify your domain at https://resend.com/domains
#    Otherwise emails send from onboarding@resend.dev (fine for testing)
RESEND_API_KEY=re_your_api_key_here
EMAIL_FROM=Nine Dart Nation <onboarding@resend.dev>

# OPTION 2: SMTP (fallback — only used if RESEND_API_KEY is not set)
# Note: Outlook/Hotmail SMTP is blocked from most cloud providers (Render, Railway, etc.)
# Gmail with App Password works in some cases.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-gmail-app-password
SMTP_FROM=Nine Dart Nation <your-email@gmail.com>
MAIL_DOMAIN=gmail.com
