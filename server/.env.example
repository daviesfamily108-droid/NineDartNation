# Server configuration
# HTTP port for Express + WS (insecure)
PORT=8787

# Enable HTTPS listener (1 to enable)
NDN_HTTPS=0
# HTTPS port (when enabled)
HTTPS_PORT=8788
# Paths to TLS key/cert if using custom locations
NDN_TLS_KEY=./certs/server.key
NDN_TLS_CERT=./certs/server.crt

# Logging level: trace|debug|info|warn|error|fatal|silent
LOG_LEVEL=info

# --- Stripe (optional if using Payment Link) ---
# Secret API key (do NOT set in Netlify). Set in Render only.
STRIPE_SECRET_KEY=
# Webhook secret if you verify events server-side (recommended)
STRIPE_WEBHOOK_SECRET=
# £2 username change price ID (provided by user)
STRIPE_PRICE_ID_USERNAME_CHANGE=price_1SC1iwRAvdcgMbFdqDMr39gu
