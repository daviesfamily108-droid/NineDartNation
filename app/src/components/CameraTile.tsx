// Re-export the canonical CameraTile component from the root `src/components` folder.
// This file exists only to avoid duplicate implementations across the workspace.
export { default } from '../../../src/components/CameraTile'
