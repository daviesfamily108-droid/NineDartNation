# Vite client environment variables
# If deploying behind a different host/port or reverse proxy, set the public WebSocket URL.
# Example: VITE_WS_URL=wss://your-domain.example/ws
VITE_WS_URL=

# API base is proxied in dev via vite.config.ts. In prod builds, the app will
# call relative /api paths against the same origin.
