# PWA icons

This folder contains the PWA icons referenced by `public/manifest.webmanifest`.

If you want a custom app icon:
- Add `icon-192.png` (192×192)
- Add `icon-512.png` (512×512)

These should be real PNG files (not data URLs), otherwise browsers may log icon download/validation errors.
