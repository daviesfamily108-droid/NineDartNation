# Example env for Nine-Dart-Nation (Vite client)
# Copy this file to app/.env (Netlify uses build-time env) or set in Netlify UI.

# API base URL used by the app for auth, user management, etc.
# On Netlify, set this to your Render service URL with https scheme, e.g.:
#   VITE_API_URL=https://your-service.onrender.com
# For local dev against a local server:
#   VITE_API_URL=http://localhost:8787
VITE_API_URL=

# WebSocket base URL used by the app.
# On Netlify, set this to your Render service URL with wss scheme, e.g.:
#   VITE_WS_URL=wss://your-service.onrender.com
# For local dev against a local server:
#   VITE_WS_URL=ws://localhost:8787
VITE_WS_URL=

# Optional: override the automatic remote fallback hosts when deploying to Netlify
# These ensure the client points at your Render backend even if VITE_API_URL/WS_URL
# are set to localhost during development builds.
VITE_REMOTE_API_FALLBACK=https://ninedartnation-1.onrender.com
VITE_REMOTE_WS_FALLBACK=wss://ninedartnation-1.onrender.com/ws

# Admin credentials for demo/demo purposes (client-side only, not secure)
# Set these in Netlify build env for production overrides
VITE_ADMIN_EMAIL=daviesfamily108@gmail.com
VITE_ADMIN_USERNAME=DartsWithG
VITE_ADMIN_PASSWORD=Cymru-2015

# Stripe Checkout URL used in SettingsPanel (Pay via Stripe button)
# Example: https://buy.stripe.com/your-live-or-test-link
VITE_STRIPE_CHECKOUT_URL=
